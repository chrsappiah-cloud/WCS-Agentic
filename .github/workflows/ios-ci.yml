# Deprecated: use ci.yml. Kept to avoid broken links; delegates to CI gate.
name: iOS CI

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use workflow 'CI' (.github/workflows/ci.yml) for all checks."
